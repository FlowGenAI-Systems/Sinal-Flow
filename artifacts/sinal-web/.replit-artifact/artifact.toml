kind = "web"
previewPath = "/"
title = "Sinal"
version = "1.0.0"
id = "artifacts/sinal-web"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24227

[services.development]
run = "pnpm --filter @workspace/sinal-web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/sinal-web", "run", "build" ]
publicDir = "artifacts/sinal-web/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24227"
BASE_PATH = "/"
